// const pastZoomLinks = JSON.parse(localStorage.getItem('zoomHistory') ?? "[]")
// const dateString = JSON.stringify( Date.now())
// pastZoomLinks.push({ dateString: window.location.href })
// if (pastZoomLinks.length > 5) pastZoomLinks.shift();
// localStorage.setItem("zoomHistory", JSON.stringify(pastZoomLinks))

// alert(JSON.stringify(pastZoomLinks))
